# --- copyrighted game data: NEVER commit (disc images + extracted game binaries) ---
Game Files/
*.bin
*.BIN
*.img
*.iso
*.cue
WILDARM2.EXE
*.EXE

# --- third-party fan translation (gadesx Spanish patch): local ground-truth reference only, do not redistribute ---
spanish_patch/

# --- OS noise ---
.DS_Store

# --- large regenerable font extraction images (keep the JSON tables) ---
font_work/*.png

# python bytecode
__pycache__/
*.pyc

# --- extracted font/VRAM dumps (copyrighted game data) ---
font replacement/
font_work/sys/

# --- web test fixtures (game data / third-party patch, local only) ---
web/tests/fixtures/*.bin
web/tests/fixtures/*.ppf


# --- migrated corpus: derived, 2 MB, and its placement quality is not shippable yet (issue #10).
#     Regenerate with tools/migrate_db_to_boxes.mjs; issue #3 decides what gets published.
data/migrated_corpus*.json
data/jp_en_alignment.json

# --- generated translation worklists (derived; rebuild with tools/build_worklist.mjs) ---
data/worklist_*.json
